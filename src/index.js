/**
 *
 * @param {number} x
 * @param {number} y
 */
function add(x, y) {
  return x + y;
}

// subtract stuff
function subtract(x, y) {
  return x - y;
}

// multiply stuff yesss!


function multiply(x, y) {
  return x * y;
}

export default add;
